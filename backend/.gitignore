.env
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# Credenciais do Google Calendar — nunca comitar
credentials.json
token.json

# Credencial do GitHub (agente Norte) — nunca comitar
github_token.json
